import React from "react";
import { CustomContainer } from "../styles/styles";
import { Typography } from "@mui/material";

const CardInfo = () => {
  return (
    <CustomContainer>
      <Typography variant="h3">Card Information</Typography>
    </CustomContainer>
  );
};

export default CardInfo;
